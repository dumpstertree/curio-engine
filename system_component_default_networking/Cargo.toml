[package]
name = "system_component_default_networking"
version = "0.1.0"
edition = "2024"

[dependencies]
core = { path = "../core" }
message-io = "0.19.0"
serde = { version = "1.0.104", features = ["derive"] }

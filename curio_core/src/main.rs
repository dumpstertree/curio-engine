use std::path::Path;

// use curio_core::engine::{curio::load_curio, curio_cabinet::CurioCabinet};

fn main() {
    // println!("trying...");
    // CurioCabinet::put_on_display(|| load_curio(Path::new("./plugins")).curio);
}
